import numpy as np


def load_mhd_file(file):
    # Placeholder function to simulate loading of an MHD file
    # Replace with actual MHD file loading logic as needed
    return f"Loaded {file.name}"


def process_coordinates_and_file(file, x, y, z):
    # Simulate processing of the file and coordinates
    # Replace this with actual logic as needed

    # Example of dummy scores for 4 models (accuracy, precision)
    scores = [(0.90, 0.85), (0.80, 0.78), (0.92, 0.88), (0.87, 0.83)]
    return scores
